{
  "name": "bucket-db",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --cwd packages/core build && bun run --filter='@hold-baby/bucket-db-*' build",
    "dev": "bun run --cwd packages/core dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
